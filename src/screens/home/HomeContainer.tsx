import React, { useState } from "react";

function Home() {
  const [count, setCount] = useState(0);

  return (
    <div className="flex container mx-auto bg-nba text-white">hello world</div>
  );
}

export default Home;
